import {Component} from "../../../types/types";
import React from "react";

const CreateContainer: React.FC<{ addChild: (child: Component) => void }> = () => {
    return (<></>)
}

export default CreateContainer